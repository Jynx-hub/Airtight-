# Person 1 data pipeline output — pulled bulk data stays local (data/real/…).
# Tracked fixtures and the committed puller scripts are unaffected (already tracked).
data/
*.duckdb
checkpoint.json

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
.pytest_cache/

# Eval output (re-derivable; transcripts can be large)
results/

# Agent-generated episodic memory (re-derivable)
memory/episodes/*.json

# Env / secrets — but DO track the config template
.env
.env.*
!.env.example
*.key
*.pem

# macOS / editors
.DS_Store
.vscode/
.idea/
