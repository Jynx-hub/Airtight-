Run the M4 ablation: `.venv/bin/python -m agent.eval --data-root data --k 5 --runs 1` from the repo root. Then read `results/ablation/latest/results.json` and summarize the per-pair deltas (loopholes-caught, drafting time, defects) and the fingerprint's mode/model. Report the chart path (`results/ablation/latest/chart.html`). If mode is "stub", say so plainly — no delta is expected until AIRTIGHT_MODE=live. Never edit metrics or results files by hand.
