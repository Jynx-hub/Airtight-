# Airtight — exact dependency set behind the recorded green test run.
#
# This is a LOCK, not a declaration. The loose declarations live in:
#   pyproject.toml   — the airtight package + [dev]/[web]/[hl] extras (the test path)
#   requirements.txt — the scripts/ + data/ ingestion pipeline
# Pinning those with == would be wrong: pyproject is an installable package's contract,
# and a hackathon repo still wants `pip install -e .` to resolve normally.
#
# Recorded 2026-07-18 on CPython 3.12.7 (macOS/arm64): `.venv/bin/pytest tests/` → 71 passed.
# Reproduce that env exactly:
#   python3.12 -m venv .venv
#   .venv/bin/pip install -r requirements-lock.txt
#   .venv/bin/pip install -e . --no-deps      # the package itself, deps already pinned above
# For a NORMAL setup you want README.md's quick start instead, not this file.
#
# Regenerate after changing a dependency:
#   .venv/bin/pip freeze --exclude-editable > requirements-lock.txt   # then restore this header
aiofiles==25.1.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.1
aiosignal==1.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.6.17
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
cryptography==49.0.0
distro==1.9.0
duckdb==1.5.4
fastapi==0.139.2
frozenlist==1.8.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jiter==0.16.0
multidict==6.7.1
openai==2.46.0
orjson==3.11.9
packaging==26.2
pdfminer.six==20260107
pdfplumber==0.11.10
pillow==12.3.0
pluggy==1.6.0
propcache==0.5.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.20.0
pypdfium2==5.12.1
pytest==9.1.1
pytest-asyncio==1.4.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
regex==2026.7.10
reportlab==5.0.0
six==1.17.0
sniffio==1.3.1
starlette==1.3.1
tqdm==4.69.0
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
yarl==1.24.2
